-- T26 动态路由：菜单项记得住自己指向的「页面实现」（组件名）。
--
-- 内置页面的 component 来自页面文件（基线 pages[].component，代码所有、管理端不可改）；
-- 管理端**新增**的菜单项（把某个页面再挂一个入口、或换个路径暴露）没有内置页面对应，
-- 必须把 component 落库，否则前端拿到 path 也不知道渲染哪个组件——这就是动态路由的落点：
-- 路径与层级是数据（可改），组件实现是代码（只能从已有页面里选）。
ALTER TABLE menu ADD COLUMN component VARCHAR(80) NULL;
